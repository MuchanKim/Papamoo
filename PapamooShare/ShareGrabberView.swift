import SwiftUI

struct ShareGrabberView: View {
    var body: some View {
        Capsule()
            .fill(.secondary)
            .frame(width: 36, height: 5)
            .accessibilityHidden(true)
    }
}
